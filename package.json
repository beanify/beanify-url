{
  "name": "beanify-url",
  "version": "1.0.0",
  "description": "beanify-plugin beanify-url beanify micro-service",
  "main": "index.js",
  "scripts": {
    "test": "tap test/*.test.js --100 --cov --coverage-report=text",
    "unit": "tap test/*.test.js --100 --cov --coverage-report=lcov"
  },
  "keywords": [
    "beanify-url",
    "beanify",
    "beanify-plugin",
    "micro-service"
  ],
  "author": "beanjs",
  "license": "MIT",
  "typings": "./index.d.ts",
  "repository": {
    "type": "git",
    "url": "git@github.com/beanjs-framework/beanify-url.git"
  },
  "dependencies": {
    "beanify-plugin": "^1.0.3"
  },
  "devDependencies": {
    "beanify": "^1.0.6",
    "tap": "^14.9.2"
  }
}
